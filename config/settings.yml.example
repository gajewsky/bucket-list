defaults: &defaults
  untappd:
    client_id: token
    client_secret: token
    username: username

  strava:
    client_secret: token
    access_token: token

  google:
    client_id: token 
    client_secret: token

  goodreads:
    key: token
    secret: token
    user_id: token

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
